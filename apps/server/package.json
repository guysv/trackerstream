{
  "name": "@trackerstream/server",
  "version": "0.0.0",
  "private": true,
  "description": "trackerstream control plane: ingest->CID-DAG pipeline, master pin, catalog->IPNS publish",
  "type": "module",
  "scripts": {
    "ingest": "node --experimental-sqlite bin/ingest.ts"
  },
  "dependencies": {
    "@trackerstream/config": "workspace:*",
    "@trackerstream/repack": "workspace:*",
    "@trackerstream/wasm": "workspace:*",
    "multiformats": "^13.3.1",
    "yauzl": "^3.4.0"
  }
}
