{
  "name": "@trackerstream/server",
  "version": "0.0.0",
  "private": true,
  "description": "trackerstream control plane: catalog/search API, ingest->CID-DAG pipeline, master pin",
  "type": "module",
  "scripts": {
    "ingest": "node --experimental-sqlite bin/ingest.ts",
    "serve": "node --experimental-sqlite bin/serve.ts"
  },
  "dependencies": {
    "@trackerstream/config": "workspace:*",
    "@trackerstream/repack": "workspace:*",
    "@trackerstream/wasm": "workspace:*",
    "multiformats": "^13.3.1",
    "yauzl": "^3.4.0"
  }
}
