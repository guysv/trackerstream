{
  "name": "@trackerstream/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared trackerstream UI: Svelte 5 components, stores, the audio engine, and the NodeClient seam both shells implement",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client/index.ts",
    "./theme.css": "./src/theme.css",
    "./*": "./src/*"
  },
  "scripts": {
    "build:worklet": "node scripts/build-worklet.mjs build/player.worklet.js",
    "test": "pnpm build:worklet && node test/fence-test.mjs && node test/worklet-harness.mjs"
  },
  "dependencies": {
    "@trackerstream/config": "workspace:*",
    "@trackerstream/wasm": "workspace:*"
  },
  "peerDependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
