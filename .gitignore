# dependencies
node_modules/

# logs / os
*.log
.DS_Store

# rust
target/

# deploy build artifacts (generated by build-artifact.sh)
deploy/dist/

# local dev-peer address (written by the find-lan-peer skill) — never shared
/DEV_PEER_ADDR

# local agent / editor config — never shared
CLAUDE.md
.claude/

# TMA API dumps: ignore the loose raw working files, but bank the dated snapshots.
api-dumps/*
!api-dumps/README.md
!api-dumps/tma-genres-*.tsv
!api-dumps/tma-genres-*.json
!api-dumps/tma-genre-raw-*.zip
