# dependencies
node_modules/

# logs / os
*.log
.DS_Store

# rust
target/

# deploy build artifacts (generated by build-artifact.sh)
deploy/dist/

# local dev-peer address (written by the find-lan-peer skill) — never shared
/DEV_PEER_ADDR

# local agent / editor config — never shared
CLAUDE.md
.claude/
